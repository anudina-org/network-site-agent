---
# 1. Namespace (The container for everything)
apiVersion: v1
kind: Namespace
metadata:
  name: agentic-space
---
# 2. Deployment (Original Resources + Tolerations)
apiVersion: apps/v1
kind: Deployment
metadata:
  name: network-agent
  namespace: agentic-space
  labels:
    app: network-agent
spec:
  replicas: 1
  selector:
    matchLabels:
      app: network-agent
  template:
    metadata:
      labels:
        app: network-agent
    spec:
      containers:
      - name: network-agent
        image: docker.io/anandms2026/network-agent:uat
        imagePullPolicy: Always
        ports:
        - containerPort: 8000
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi
      tolerations:
      - key: "node-role.kubernetes.io/control-plane"
        operator: "Exists"
        effect: "NoSchedule"
---
# 3. Service
apiVersion: v1
kind: Service
metadata:
  name: network-agent
  namespace: agentic-space
  labels:
    app: network-agent
spec:
  ports:
  - port: 8000
    targetPort: 8000
  selector:
    app: network-agent
---
# 4. Route (Nip.io for Mac Access)
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: network-agent
  namespace: agentic-space
  labels:
    app: network-agent
spec:
  host: network-agent.192.168.130.11.nip.io
  to:
    kind: Service
    name: network-agent
  port:
    targetPort: 8000
---
# 5. NetworkPolicy (The "Unlock" for External Traffic)
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-from-ingress
  namespace: agentic-space
spec:
  podSelector:
    matchLabels:
      app: network-agent
  ingress:
  - from:
    - namespaceSelector:
        matchLabels:
          network.openshift.io/policy-group: ingress
  policyTypes:
  - Ingress